server = ser
client = cln
version = [type, ,number]
end = :

Connect to server
cln     connect 'version cln':
ser     connect 'version ser':

List version
cln     get list version:
ser     return list version:

Download version
cln	download 'version':
ser	upload 'version':